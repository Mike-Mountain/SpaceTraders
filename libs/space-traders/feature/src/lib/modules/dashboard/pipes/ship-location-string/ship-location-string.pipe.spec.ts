import { ShipLocationStringPipe } from './ship-location-string.pipe';

describe('ShipLocationStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ShipLocationStringPipe();
    expect(pipe).toBeTruthy();
  });
});
