import { ContractDescriptionPipe } from './contract-descrtiption.pipe';

describe('ContractDescrtiptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
