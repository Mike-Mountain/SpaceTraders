import { HasShipyardPipe } from './has-shipyard.pipe';

describe('HasShipyardPipe', () => {
  it('create an instance', () => {
    const pipe = new HasShipyardPipe();
    expect(pipe).toBeTruthy();
  });
});
