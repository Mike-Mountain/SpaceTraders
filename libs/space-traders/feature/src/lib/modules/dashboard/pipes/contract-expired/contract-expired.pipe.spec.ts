import { ContractExpiredPipe } from './contract-expired.pipe';

describe('ContractExpiredPipe', () => {
  it('create an instance', () => {
    const pipe = new ContractExpiredPipe();
    expect(pipe).toBeTruthy();
  });
});
