import { GetPlanetImagePipe } from './get-planet-image.pipe';

describe('GetPlanetImagePipe', () => {
  it('create an instance', () => {
    const pipe = new GetPlanetImagePipe();
    expect(pipe).toBeTruthy();
  });
});
